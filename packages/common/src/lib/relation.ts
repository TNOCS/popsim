export enum RelationType {
  unknown,
  live,
  work,
  learn,
  /** Visit to shop, talk to someone, etc. */
  visit,
  /** kiss & ride, e.g. when dropping off or picking up the kids from school, sports etc. */
  kissAndRide,
  relax
}
